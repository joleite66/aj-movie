import React from "react";

function Posters(props) {
    return(
        props.forEach(el => {
            <img src="el" alt="poster"/>
            
        })
        
    );
}

export default Posters;